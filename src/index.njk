---
layout: default
title: grant frazier
htmlClass: html
bodyClass: body
---

<div class="home-container">
 <div class="title-block">
   <h1 class="name">grant frazier</h1>
   <p> // twtr: <a href="https://x.com/gantisdant">@gantisdant</a></p>
   <p> // instgrm: <a href="https://instagram.com/gant.fr">@gant.fr</a></p>
   <p> // blsky: <a href="https://bsky.app/profile/gant.foo">@gant.foo</a></p>
 </div>

 <h2>published poems</h2>
 <ul class="work-list">
 {% for poem in collections.poems %}
 <li>
   <a href="{{ poem.url }}">{{ poem.data.title }}</a>
   <div class="meta-info">
     {% if poem.data.publicationUrl %}
       <a href="{{ poem.data.publicationUrl }}">{{ poem.data.publication }}</a>
     {% else %}
       <span>{{ poem.data.publication }}</span>
     {% endif %}
     <span class="separator"> // </span>
     <span>{{ poem.data.date | format("MMM yyyy") }}</span>
   </div>
 </li>
 {% endfor %}
 </ul>

<h2>published essays</h2>
 <ul class="work-list">
 {% for essay in collections.essays %}
   <li>
     <a href="{{ essay.url }}">{{ essay.data.title }}</a>
     <div class="meta-info">
       {% if essay.data.publicationUrl %}
         <a href="{{ essay.data.publicationUrl }}" class="publication">{{ essay.data.publication }}</a>
       {% else %}
         <span class="publication">{{ essay.data.publication }}</span>
       {% endif %}
       <span class="date">// {{ essay.data.date | format("MMM yyyy") }}</span>
     </div>
   </li>
 {% endfor %}
 </ul>
</div>

<h2>selected past work</h2>
<p>past writing that's less representative of my current voice. mostly articles/essays about transit and local issues.</p>
 <ul class="work-list">
 {% for article in collections.articles %}
   <li>
     <a href="{{ article.url }}">{{ article.data.title }}</a>
     <div class="meta-info">
       {% if article.data.publicationUrl %}
         <a href="{{ article.data.publicationUrl }}" class="publication">{{ article.data.publication }}</a>
       {% else %}
         <span class="publication">{{ article.data.publication }}</span>
       {% endif %}
       <span class="date">// {{ article.data.date | format("MMM yyyy") }}</span>
     </div>
   </li>
 {% endfor %}
 </ul>
</div>
